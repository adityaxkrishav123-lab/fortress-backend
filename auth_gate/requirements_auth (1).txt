# Identity Shield — Auth Gate Dependencies
# Pin these versions to avoid breaking changes

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic[email]>=2.7.0
passlib[argon2]>=1.7.4
PyJWT>=2.8.0
cryptography>=42.0.0          # Required by PyJWT for RS256
httpx>=0.27.0                 # Async HTTP for fetching Firebase public keys
firebase-admin>=6.5.0         # Firestore + Storage + Auth Admin SDK
python-multipart>=0.0.9       # File uploads (UploadFile)
